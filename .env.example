# Optional; demo mode works without a key. Never commit your real .env.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# Leave empty for data/taskup.db next to database.py.
TASKUP_DB=
# Optional translation cache folder; empty uses .cache/translations in the project.
TASKUP_TRANSLATION_CACHE_DIR=
